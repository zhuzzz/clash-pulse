{
  "name": "clash-pulse",
  "version": "1.0.0",
  "description": "Trigger a Clash delay check and auto-select the lowest-latency matching node via the external-controller API.",
  "type": "module",
  "bin": {
    "clash-pulse": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "watch": "node index.js --watch",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
